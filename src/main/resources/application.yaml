spring:
  application:
    name: jackson-spreadsheet-examples
  # Uncomment to allow large Excel file uploads (default: 1MB)
  #servlet:
  #  multipart:
  #    max-file-size: 50MB
  #    max-request-size: 50MB
